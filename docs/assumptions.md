# Assumptions

- Specific ailment waiting period overrides generic PED waiting period.
- MRI and CT may require pre-auth from derived rule assumptions.
- Per-claim limit is enforced as a hard cap on the approved payout (partial approval up to the cap).
- Fraud indicators trigger manual review unless there is clear invalidation evidence.
- Consultation co-pay is applied only on consultation fees (not the entire claim total).
